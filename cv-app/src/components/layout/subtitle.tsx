import SubtitleLayout from "../theme-components/subtitle-layout";

export default function Subtitle({ children }: { children: React.ReactNode }) {
  return <SubtitleLayout>{children}</SubtitleLayout>;
}
