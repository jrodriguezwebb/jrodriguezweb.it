import ServicesLayout from "./theme-components/services-layout";

export default function Services({ children }: { children: React.ReactNode }) {
  return <ServicesLayout>{children}</ServicesLayout>;
}
